diff --git a/src/transcribe.cpp b/src/transcribe.cpp
index dfeb5fb..32c9430 100644
--- a/src/transcribe.cpp
+++ b/src/transcribe.cpp
@@ -1147,6 +1147,39 @@ static bool starts_with_bytes(const std::string & s, const std::string & prefix)
     return s.size() >= prefix.size() && s.compare(0, prefix.size(), prefix) == 0;
 }
 
+// Bytes of full_text covered by prefix, allowing full_text to have dropped
+// the single leading space a word-boundary token decodes to.
+static size_t token_prefix_match_bytes(const std::string & full_text, const std::string & prefix) {
+    if (starts_with_bytes(full_text, prefix)) {
+        return prefix.size();
+    }
+    if (!prefix.empty() && prefix.front() == ' ' && (full_text.empty() || full_text.front() != ' ')) {
+        const std::string normalized_prefix = prefix.substr(1);
+        if (starts_with_bytes(full_text, normalized_prefix)) {
+            return normalized_prefix.size();
+        }
+        return common_prefix_bytes(full_text, normalized_prefix);
+    }
+    return common_prefix_bytes(full_text, prefix);
+}
+
+// Collapse runs of ASCII spaces to one, the same way families such as
+// parakeet normalize full_text.
+static std::string collapse_space_runs(const std::string & s) {
+    std::string out;
+    out.reserve(s.size());
+    bool prev_space = false;
+    for (size_t i = 0; i < s.size(); ++i) {
+        const bool is_space = s[i] == ' ';
+        if (is_space && prev_space) {
+            continue;
+        }
+        out.push_back(s[i]);
+        prev_space = is_space;
+    }
+    return out;
+}
+
 static size_t token_prefix_raw_bytes(const transcribe_session * session, int n_tokens) {
     if (session == nullptr || n_tokens <= 0 || session->tokens.empty()) {
         return 0;
@@ -1157,17 +1190,14 @@ static size_t token_prefix_raw_bytes(const transcribe_session * session, int n_t
     for (int i = 0; i < capped; ++i) {
         prefix += session->tokens[static_cast<size_t>(i)].text;
     }
-    if (starts_with_bytes(session->full_text, prefix)) {
-        return prefix.size();
-    }
-    if (!prefix.empty() && prefix.front() == ' ' && (session->full_text.empty() || session->full_text.front() != ' ')) {
-        const std::string normalized_prefix = prefix.substr(1);
-        if (starts_with_bytes(session->full_text, normalized_prefix)) {
-            return normalized_prefix.size();
-        }
-        return common_prefix_bytes(session->full_text, normalized_prefix);
-    }
-    return common_prefix_bytes(session->full_text, prefix);
+    // Per-token texts are decoded in isolation, while full_text may have had
+    // space runs collapsed. A token that decodes to a bare space before a
+    // word-boundary token then leaves a double space in the concatenation
+    // that full_text never contains, and without the collapsed comparison the
+    // boundary could never advance past that point again.
+    const size_t exact     = token_prefix_match_bytes(session->full_text, prefix);
+    const size_t collapsed = token_prefix_match_bytes(session->full_text, collapse_space_runs(prefix));
+    return std::max(exact, collapsed);
 }
 
 static size_t family_candidate_raw_prefix_bytes(const transcribe_session * session) {
diff --git a/tests/stream_dispatch_unit.cpp b/tests/stream_dispatch_unit.cpp
index fa0df5e..c7fb887 100644
--- a/tests/stream_dispatch_unit.cpp
+++ b/tests/stream_dispatch_unit.cpp
@@ -828,6 +828,54 @@ void test_stream_family_token_prefix_maps_trimmed_leading_space() {
     CHECK(std::strcmp(text.tentative_text, "") == 0);
 }
 
+// A token that decodes to a bare space before a word-boundary token puts a
+// double space into the token concatenation, while parakeet's full_text has
+// space runs collapsed. The boundary must still advance, on that feed and on
+// every later one.
+void test_stream_family_token_prefix_maps_collapsed_space_run() {
+    transcribe_model model;
+    init_streaming_model(model);
+    model.arch = &sequence_arch_named_parakeet();
+    transcribe_session session;
+    session.model = &model;
+
+    g_feed_texts[0]             = "hello world";
+    g_feed_texts[1]             = "hello world again";
+    g_n_feed_texts              = 2;
+    g_n_token_rows              = 3;
+    g_token_texts[0]            = " hello";
+    g_token_texts[1]            = " ";
+    g_token_texts[2]            = " world";
+    g_forced_n_committed_tokens = 3;
+
+    CHECK(transcribe_stream_begin(&session, nullptr, nullptr) == TRANSCRIBE_OK);
+
+    float                    pcm = 0.0f;
+    transcribe_stream_update upd;
+    transcribe_stream_update_init(&upd);
+    CHECK(transcribe_stream_feed(&session, &pcm, 1, &upd) == TRANSCRIBE_OK);
+    CHECK(upd.committed_changed);
+
+    transcribe_stream_text text;
+    transcribe_stream_text_init(&text);
+    CHECK(transcribe_stream_get_text(&session, &text) == TRANSCRIBE_OK);
+    CHECK(std::strcmp(text.committed_text, "hello world") == 0);
+    CHECK(std::strcmp(text.tentative_text, "") == 0);
+
+    g_n_token_rows              = 4;
+    g_token_texts[3]            = " again";
+    g_forced_n_committed_tokens = 4;
+
+    transcribe_stream_update_init(&upd);
+    CHECK(transcribe_stream_feed(&session, &pcm, 1, &upd) == TRANSCRIBE_OK);
+    CHECK(upd.committed_changed);
+
+    transcribe_stream_text_init(&text);
+    CHECK(transcribe_stream_get_text(&session, &text) == TRANSCRIBE_OK);
+    CHECK(std::strcmp(text.committed_text, "hello world again") == 0);
+    CHECK(std::strcmp(text.tentative_text, "") == 0);
+}
+
 void test_stream_text_stable_prefix_does_not_rewrite_committed() {
     transcribe_model model;
     init_streaming_model(model);
@@ -1572,6 +1620,7 @@ int main() {
     test_stream_auto_unknown_family_falls_back_to_generic_stable_prefix();
     test_stream_stable_prefix_known_family_uses_family_boundary();
     test_stream_family_token_prefix_maps_trimmed_leading_space();
+    test_stream_family_token_prefix_maps_collapsed_space_run();
     test_stream_text_stable_prefix_does_not_rewrite_committed();
     test_stream_text_clamps_raw_tentative_offset_after_shrink();
     test_stream_committed_row_hints_can_exceed_current_rows();
